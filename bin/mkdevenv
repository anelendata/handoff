#!/bin/bash

mkdir -p venv

# dev
python3 -m venv ./venv/dev
/bin/bash -c "source ./venv/dev/bin/activate && pip install wheel && deactivate"
/bin/bash -c "source ./venv/dev/bin/activate && pip install -r reqs/requirements.txt && pip install -r reqs/requirements_dev.txt && deactivate"
